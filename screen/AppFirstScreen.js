import { Text, View } from "react-native"

const AppFirstScreen = ()=>{
    return(
        <View style={{flex:1, alignItems:"center", justifyContent:"center"}}>
            <Text> First Screen</Text>
        </View>
    )
}

export default AppFirstScreen